name: Telegram Receiver (Reusable)

on:
  workflow_call:
    secrets:
      ANTHROPIC_API_KEY:
        required: true
      TELEGRAM_BOT_TOKEN:
        required: true
      TELEGRAM_CHAT_ID:
        required: true
      GH_PAT:
        required: true

jobs:
  receive:
    name: Process incoming Telegram commands
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Test
        run: echo "Hello"
